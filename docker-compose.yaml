services:
  redis:
    image: redis:alpine
    container_name: lokalni_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_podaci:/data

volumes:
  redis_podaci: